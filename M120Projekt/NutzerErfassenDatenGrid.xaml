<UserControl x:Class="M120Projekt.NutzerErfassenDatenGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:M120Projekt"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name="dgNutzer" ItemsSource="{Binding nutzer}" AutoGenerateColumns="False" MouseDoubleClick="DgNutzer_MouseDoubleClick" SelectionChanged="DgNutzer_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nachname" Binding="{Binding Nachname}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Buchtitel" Binding="{Binding Buecherliste}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ausleihedatum" Binding="{Binding Ausleihedatum}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
